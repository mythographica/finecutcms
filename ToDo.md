# FineCut Admin Panel — Remaining Tasks

## Completed (2025-05-04)

- [x] Port admin panel frontend (HTML/CSS/JS) from `_adm/` to `public/`
- [x] Fix runtime errors: style.css 404, elFinder 500, POST 415/400/404
- [x] Fix Fastify route ordering conflict (catch-all before API routes)
- [x] Fix Properties tab `[object Object]` — `getPage()`/`setPage()` now return raw strings for `info`/`blocks` matching PHP behavior
- [x] Build: 0 errors, Tests: 49/49 passing

## Completed (2025-05-05)

- [x] **elFinder / Files Manager tab** — Backend handler tested with 6 integration tests (`test/integration/elfinder.test.ts`). All commands (open, mkdir, mkfile, rm, ls, tree, get, put, rename, paste) functional.
- [x] **Templates tab empty dropdown** — Fixed missing `<select>` element in `admin.html` that was previously generated by PHP `includeTemplates()`. Added static HTML `<select id="treePagesEditor-template">` so `loadTemplates()` in `startup.js` can populate it.

## Completed (2025-05-08)

- [x] Added generic `hooksOpts<P, T>` to mnemonica core
- [x] Rebuilt mnemonica core — 568 passing, 100% coverage
- [x] Updated `pino-logger.ts` and `server.ts` hooks to use typed `hooksOpts` — eliminated casts
- [x] Added `.exception()` enrichment in `frontend.ts`
- [x] Rewrote all Node.js documentation content (10 pages)
- [x] Rewrote `README.txt` with architecture overview
- [x] Cleared `data/static/*` for fresh cache generation
- [x] Created `.ai/` agent documentation folder (AGENTS.md, ONBOARDING.md, ARCHITECTURE.md, API.md, FAQ.md)
- [x] Fixed 6 eslint errors (line length + unused variables)

## Upcoming

(none — project is in maintenance mode)
